[project]
name = "ollamastudio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Deepa Rajasabeson", email = "deeparajasabeson@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.2",
    "openai>=3.7.0",
    "pymupdf>=1.28.2",
]

[project.scripts]
ollamastudio = "ollamastudio:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
